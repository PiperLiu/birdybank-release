现在可以在软件内重命名、删除、移动照片！支持批量操作；引入重复照片检测与合并功能；优化UI交互；优化仪表盘。

You can now rename, delete, and move photos within the app! Supports batch operations; introduces duplicate photo detection and merging; improves UI interactions; optimizes the dashboard.
